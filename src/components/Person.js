import React from 'react'

function Person({person}) {
  return (
    <div>
      {/* <h3>{person.id}:I am {person.name} {person.age} years old.And I know{person.skill}
    </h3> */}
    </div>
  )
}

export default Person
